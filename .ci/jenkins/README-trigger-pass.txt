This is a dummy file which introduces a change on my branch chrisl/gen-ci-pass.
It exists to test the CI system and generate documentation.  This branch will
never be merged, and will be deleted once the documentation is done.
- ChrisL
